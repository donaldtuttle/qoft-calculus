{
  "name": "@qoft-calculus/mw-agent-scaffold",
  "version": "0.1.0",
  "private": true,
  "description": "DEVELOP Architecture A wrapper: MW vs EMA vs stateless delayed-cue harness. Does not modify Memory Weather v0.1.1.",
  "scripts": {
    "lock": "node scripts/generate-lock.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/index.js && node --check src/encode.js && node --check src/runner.js && node --check src/harness.js",
    "manifest": "node scripts/write-manifest.js",
    "verify": "npm run check && npm test && npm run manifest"
  },
  "engines": {
    "node": ">=20"
  }
}

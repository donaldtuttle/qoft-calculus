{
  "name": "qoft-calculus",
  "version": "1.0.2",
  "private": true,
  "type": "module",
  "description": "Typed recursive state-update calculus with a deterministic R¹² TypeScript reference engine, ablations, and agent skill contract.",
  "scripts": {
    "test": "node --experimental-strip-types tests/determinism.ts",
    "typecheck": "tsc --noEmit",
    "run": "node --experimental-strip-types -e \"import { run } from './src/engine.ts'; const r = run(32, '0x51e1d'); console.log(r.frames.at(-1));\""
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
